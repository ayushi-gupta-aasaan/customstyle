import colors from "./colors.config";

const globalStyles = {
  defaultFontFamily:
    "Roboto,-apple-system,BlinkMacSystemFont,Segoe UI,Helvetica Neue,Helvetica,arial,sans-serif",
  colors
};

export default globalStyles;
